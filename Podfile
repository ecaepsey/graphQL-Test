# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS Swift Graphql' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iOS Swift Graphql
  
   
   pod 'Apollo', '~> 0.51.0'
  target 'iOS Swift GraphqlTests' do
    
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS Swift GraphqlUITests' do
    # Pods for testing
  end

end
